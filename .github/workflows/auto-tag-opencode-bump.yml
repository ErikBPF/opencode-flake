name: Auto-tag opencode package bumps

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Extract packaged opencode version and push tag
        run: |
          set -euo pipefail
          version=$(sed -n 's/^  version = "\([^"]*\)";/\1/p' packages/opencode/package.nix | head -1)
          if ! printf '%s\n' "$version" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "current opencode package version is malformed: $version" >&2
            exit 0
          fi
          tag="opencode-v$version"
          if git rev-parse "refs/tags/$tag" >/dev/null 2>&1; then
            echo "tag $tag already exists; skipping" >&2
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git tag -a "$tag" -m "opencode $version"
          git push origin "refs/tags/$tag"
